# Frozen v4 acceptance matrix

<!-- BEGIN V4 AUTHORITY BINDING -->
### Authority binding

This document is governed by the v4 authority-binding contract. Before any compatibility chat, the compatibility plan, full plan, and conditional GO must bind the exact frozen acceptance-matrix hash, authority-contract hash, external source-manifest hash, tag-blob comparison hash, completed eight-card audit hash, and pre-GO read-only runtime-identity hash. Audit A and identity I each bind M/X and the identical v4 annotated tag object and peeled commit; A also binds the registry and every case-card tagged blob OID and SHA-256. The offline validator checks only local bytes and has no network, model, runner, paper-builder, review, release, or outreach action. PDF QA dependencies are optional outside the isolated v4-paper CI job.
<!-- END V4 AUTHORITY BINDING -->

This is a pre-freeze contract candidate. Its final hash is governed only after
independent review, a clean protocol release, and the source audit. No model
call is authorized by this file.

| Control | Required result |
| --- | --- |
| New panel | Exactly eight v4 registry cases. Each completed source-audit entry has `decision: ACCEPT`, a nonempty case-specific reason, and its own strict UTC `reviewed_at_utc` timestamp. |
| No overlap | X is re-derived from exact v3/v4 annotated-tag blobs and binds full v3 and v4 ID/entity-identifier/corpus-ID/prompt-hash sets with empty intersections for IDs, entity identifiers, corpus IDs, prompt hashes, and record-text hashes. |
| V3 exclusion | `included_count` is zero for v3 cases, trajectories, raw bytes, scores, rates, and projections. |
| Schema | `query` is a required nonempty string; `date` is optional only when an exact `YYYY-MM-DD` string equal to the case `as_of`; no other key or type is accepted. |
| Date isolation | The optional tool-call `date` cannot affect retrieval, scoring, item eligibility, or the paired result. |
| Compatibility | Before the first of exactly two excluded traces, require `compatibility_plan.acceptance_matrix_sha256 == full_plan.acceptance_matrix_sha256 == conditional_go.acceptance_matrix_sha256 == SHA256(ACCEPTANCE_MATRIX.md)`; then produce four chats using the production request/schema/transcript path. |
| Main topology | Only after both compatibility traces pass: 64 valid primary trajectories and 128 main chats, with zero development trajectories. |
| Main factors | `8 cases x 2 modes x 2 pinned models x 2 repetitions`; every case is paired by model and repetition. |
| Failure behavior | Any invalid compatibility or main trajectory leaves the campaign operationally unsealed; no resume, imputation, deletion, partial analysis, or same-authority retry. |
| Outcome neutrality | A complete replay-valid result is reportable for positive, zero, or negative paired direction. Operational failure is not a scientific zero or negative finding. |
| Output boundary | Empirical prose, tables, figures, metadata, and archive contents derive only from the answer-free verified projection. |
| Review | Exactly the ten frozen internal review lenses bind unchanged candidate bytes before local release. |
| Authority | Before compatibility, the compatibility plan, full plan, and conditional GO must bind the same exact acceptance-matrix hash, completed source-audit hash, comparison-projection hash, and external source-manifest hash. The source manifest must independently bind the reviewed v4 tag, tag object, peeled commit, local and remote release parity, preserved v3 `master` and tag identity, and every governed source blob. One later conditional Lester GO authorizes the compatibility gate and, only on its pass, the main panel. It authorizes no external action. |
| Admission and response bounds | C/F/G/A/I/M/X are external regular files and the absent external evidence root is admitted reparse-safely before output or transport; repository-local files, symlinks, and Windows junctions fail closed. Native HTTP responses are streamed under a 1,048,576-byte per-response limit and an 8,388,608-byte campaign limit. Over-limit responses produce a typed `resource` failure with at most a 4,096-byte raw prefix. |
| Self-custody limit | Self-custody evidence supports internal consistency and byte replay; it detects missing, partial, malformed, or inconsistent artifacts, including re-signed artifacts without corresponding authority. It provides no independent raw-response provenance and cannot detect a coherent rewrite of every locally held artifact. |
| Limits | Synthetic finite-panel evidence does not establish population behavior, answer correctness, live-web behavior, independent authenticity, or a model ranking. |

## Frozen review lenses

1. `claim-evidence-anti-fabrication`
2. `experimental-design-primary-development`
3. `trace-protocol-leakage-definition`
4. `finite-panel-statistical-reporting`
5. `reproducibility-provenance-determinism`
6. `related-work-novelty`
7. `plain-language-readability-abstract`
8. `adversarial-overclaim-limitations`
9. `authorship-ai-licensing-integrity`
10. `pdf-latex-arxiv-metadata-layout`
