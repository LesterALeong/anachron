"""Anachron — point-in-time tool-call leakage scoring for LLM agents.

The leakage core (:mod:`anachron.core`) is pure stdlib and importable without any
third-party dependency. The :mod:`anachron.inspect` adapter layer integrates with
`inspect_ai` and is only required when running the eval.
"""

__version__ = "0.0.1"
