# V4 claim-to-evidence map

<!-- BEGIN V4 AUTHORITY BINDING -->
### Authority binding

This document is governed by the v4 authority-binding contract. Before any compatibility chat, the compatibility plan, full plan, and conditional GO must bind the exact frozen acceptance-matrix hash, authority-contract hash, external source-manifest hash, tag-blob comparison hash, completed eight-card audit hash, and pre-GO read-only runtime-identity hash. Audit A and identity I each bind M/X and the identical v4 annotated tag object and peeled commit; A also binds the registry and every case-card tagged blob OID and SHA-256. The offline validator checks only local bytes and has no network, model, runner, paper-builder, review, release, or outreach action. PDF QA dependencies are optional outside the isolated v4-paper CI job.
<!-- END V4 AUTHORITY BINDING -->

| Permitted claim | Required evidence | Excluded material |
| --- | --- | --- |
| V4 finite-panel TCLR by model and mode | Manifest-bound v4 first-request and tool-result artifacts replayed by the frozen v4 protocol | Terminal answers, raw model prose, and v3 evidence |
| Paired TCLR direction | Answer-free projection keyed by `(case_id, model, repetition)` | Inferential statistics and model-ranking language |
| Query, restatement, and survivorship diagnostics | The same v4 projection, reported separately from TCLR | Any addition of diagnostics into the TCLR numerator |
| V4 case accounting | Frozen v4 registry, source audit, full plan, and replayed trajectory identities | Development or substituted cases; v3 cases |
| Operational completeness | Exact acceptance-matrix and external source-manifest equality across compatibility plan, full plan, and conditional GO; then conditional compatibility receipt and main manifest closure | Treating an incomplete run as a result |

No claim about answer quality, live systems, population prevalence, external
authenticity, or researcher endorsement is permitted.

Self-custody evidence supports internal consistency and byte replay; it detects
missing, partial, malformed, or inconsistent artifacts, including re-signed
artifacts without corresponding authority. It provides no independent
raw-response provenance and cannot detect a coherent rewrite of every locally
held artifact.
