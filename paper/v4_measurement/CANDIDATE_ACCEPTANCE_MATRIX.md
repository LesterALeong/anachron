# Anachron v4 candidate and local-release acceptance matrix

<!-- BEGIN V4 AUTHORITY BINDING -->
### Authority binding

This document is governed by the v4 authority-binding contract. Before any compatibility chat, the compatibility plan, full plan, and conditional GO must bind the exact frozen acceptance-matrix hash, authority-contract hash, external source-manifest hash, tag-blob comparison hash, completed eight-card audit hash, and pre-GO read-only runtime-identity hash. Audit A and identity I each bind M/X and the identical v4 annotated tag object and peeled commit; A also binds the registry and every case-card tagged blob OID and SHA-256. The offline validator checks only local bytes and has no network, model, runner, paper-builder, review, release, or outreach action. PDF QA dependencies are optional outside the isolated v4-paper CI job.
<!-- END V4 AUTHORITY BINDING -->

This is an offline pre-outcome paper contract. It does not authorize a model
call, researcher contact, endorsement-code transmission, arXiv upload, or
submission. Synthetic QA may use only disposable fixture roots containing no
real-model output and no retained candidate or release artifact.

| Gate | Pass condition |
| --- | --- |
| Protocol identity | Evidence admits only the reviewed annotated v4 tag, exact tag object, peeled commit, external source manifest, acceptance matrix, plans, registry, completed source audit, comparison projection, and conditional GO. The external manifest binds local and remote branch/tag parity, preserved v3 `master` and tag identity, and every governed source blob. Before compatibility, `compatibility_plan.acceptance_matrix_sha256 == full_plan.acceptance_matrix_sha256 == conditional_go.acceptance_matrix_sha256 == SHA256(ACCEPTANCE_MATRIX.md)` and all three source-manifest hashes are identical. |
| Conditional topology | Two excluded compatibility traces produce four chats first. Only a complete valid compatibility receipt permits exactly 64 valid primary trajectories and 128 main chats. |
| Panel scope | The projection reports eight primary cases, zero development cases, two models, two modes, and two repeated traces per pairing. |
| V3 exclusion | The projection, paper prose, metadata, tables, figures, and archive include zero v3 cases, traces, responses, scores, rates, or derived results. X mechanically proves empty v3/v4 intersections for IDs, entity identifiers, corpus IDs, prompt hashes, and record-text hashes from pinned tag blobs. The paper may disclose that v3 motivated the new interface contract. |
| Tool schema | The result contract records `query` as required and nonempty; optional `date` must exactly equal the case `as_of`; the date is not a retrieval or scoring input. |
| Answer exclusion | Candidate construction never opens terminal answers, raw model prose, GO records, source-audit prose, reviews, or approvals for publication content. |
| Generated results | Abstract results, prose, tables, figure values, captions, metadata, and result claims are generated only from the verified answer-free projection. No result field is hand-authored. The candidate receipt binds the exact metadata bytes and the canonical complete inventory of every rendered PDF page. |
| Outcome neutrality | Complete replay-valid results are candidate-eligible for positive, zero, or negative `unrestricted TCLR - enforced TCLR`. Incomplete or operationally invalid campaigns are not scientific findings. |
| Frozen prose | The manuscript template supplies exactly one generated sentence form for each positive, zero, and negative paired direction, plus a completeness sentence used only for a complete replay-valid campaign. |
| Reporting | TCLR is `post_cutoff_returned_interactions / total_interactions`. Query, restatement, and survivorship are separate diagnostics. Repeated traces are not independent observations; no significance, population, answer-quality, live-web, or model-ranking claim is permitted. |
| Self-custody limit | Self-custody evidence supports internal consistency and byte replay; it detects missing, partial, malformed, or inconsistent artifacts, including re-signed artifacts without corresponding authority. It provides no independent raw-response provenance and cannot detect a coherent rewrite of every locally held artifact. |
| Static closure | The candidate contract binds every static paper artifact, planned v4 paper tool, and planned v4 paper test through the tag-governed source manifest. The candidate contract does not hash itself or future dynamic evidence, preventing a circular post-tag hash. |
| Archive | The source archive has only the frozen allowlisted files, normalizes paths and metadata, recompiles to the reviewed PDF bytes, and matches the candidate contract's `archive_allowlist_sha256`. Its byte cap and streaming digest are checked before opening the ZIP central directory. |
| Internal review | Exactly one canonical approved review binds unchanged candidate bytes for every one of the ten frozen lens IDs. Each report's direction must equal the pooled unrestricted-minus-enforced TCLR direction derived from sealed projection cells. Missing, duplicate, stale, renamed, or publication-raced reviews fail. |
| Lester approval | A distinct post-review approval binds the candidate receipt, projection, PDF, archive, metadata, disclosure, and review set. It authorizes local release only. |
| Local release | A create-only local release copies reviewed PDF and source archive bytes; it makes no external call and does not authorize outreach, endorsement-code transmission, upload, or submission. |
| UNSENT outreach | A separate create-only renderer accepts only a valid local release and emits an exact candidate-bound `UNSENT.md` and canonical receipt. It has no recipient field and no send, browser, HTTP, upload, or submission capability. |
| CI replay | The v4 paper job installs the paper extras and runs the candidate, review, local-release, outreach, authority, source-manifest, and materialization tests on Python 3.10, 3.11, and 3.12 using only disposable fake-origin fixtures and the receipt-pinned Linux Tectonic 0.17.0 binary. A no-site-packages discovery regression keeps pure v4 static and contract coverage importable while explicitly skipping PDF-dependent cases. |

## Frozen review lenses

1. `claim-evidence-anti-fabrication`
2. `experimental-design-primary-development`
3. `trace-protocol-leakage-definition`
4. `finite-panel-statistical-reporting`
5. `reproducibility-provenance-determinism`
6. `related-work-novelty`
7. `plain-language-readability-abstract`
8. `adversarial-overclaim-limitations`
9. `authorship-ai-licensing-integrity`
10. `pdf-latex-arxiv-metadata-layout`
