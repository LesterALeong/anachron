# Pending source curation

There is no populated v2 pending draft. Existing raw v1 Wikipedia discovery artifacts are not v2 evidence. Each candidate must be freshly revalidated into the exact `routes-v2-pending-draft` schema before a human decision template can be generated. This directory must not contain fabricated PASS decisions or outcome-bearing model data.
