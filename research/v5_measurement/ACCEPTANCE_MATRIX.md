# V5 successor v6 acceptance matrix

V5 is an offline isolated-startup repair candidate. V4 remains immutable
failed-capture evidence. A future campaign requires a separate, fresh, explicit
GO and is not operationally qualified by this matrix.

| Control | Required evidence |
| --- | --- |
| Release identity | Clean annotated `v5-measurement-protocol-v6` tag, peeled commit, branch, remote references, governed blob IDs, and worktree bytes agree. |
| V4 failure binding | The exact six-file V4 failure root and its failed status remain byte-identical. |
| V5 failure binding | The consumed V5 six-file failed attempt, its release identities, and its successful cleanup status remain byte-identical historical evidence. |
| Isolated startup and containment | A private Windows Job Object with `KILL_ON_JOB_CLOSE` contains the suspended isolated root before identity binding or resume. Only bounded, task-owned no-model startup descendants may settle; a 2-second child-free interval and exactly one active Job member precede either API request. |
| Governed closure | The sorted 49-member scientific closure includes the V4 and V5 failure bindings, controller, Authenticode helper, CIM process-identity helper, controller test, and Python package bootstrap; the authority contract matches it byte-for-byte. |
| Carry-forward | All eight registry/card bytes, hashes, and accepted-v4 tag blob IDs match. |
| Materialization | Exactly seven create-only members pass bounded topology, ADS, reparse, member, total-byte, and atomic-publication checks. |
| Runtime and schedule binding | Receipt v3 binds the actual raw runtime and schedule SHA-256 values; admission checks the raw bytes and canonical runtime and schedule semantics. |
| GO and PENDING | GO validation remains GO-only. PENDING has empty authorization metadata and the fixed non-authorizing statement; `--pending-only` validates it without measurement. |
| Identity controller | A caller-owned monotonic lifecycle records every Job, root, handle, pipe, and drain acquisition before its next fallible step. Root ownership requests `0x0802`, records the immediate owner-query Win32 error, and fails closed on an unavailable or foreign owner. Its fact-only restoration predicate permits normal restoration only after the applicable cleanup truth-table row is complete; the persisted status retains the primary error and append-only cleanup receipts. |
| Lifecycle fault matrix | Literal controller rows J01, J02, J03, J04, J05, J06, J07, J08, J09, J10, J11, J12, J13, J14, J15, J16, J17, J18, J19, J20, J21, J22, J23, J24, and J25 all execute. They cover each CreateJob-to-launch-return boundary, successful `ResumeThread` followed by failed `CloseHandle`, actual no-root/unassigned/assigned teardown dispatch, every cleanup-failure class, the exact production helper trace, persisted failure receipts, and one fully successful assigned-Job restoration. |
| Measurement science | Date isolation, continuation, custody, replay, compatibility exclusion, schemas, models, PENDING semantics, and the frozen seed namespace remain governed and unchanged; `v4_included_count = 0`. |
| Release rehearsal | Before tagging, a clean detached local-Git rehearsal builds and validates a create-only source manifest from the actual governed tuple. After tagging, the remote tag repeats that validation before a separately authorized future runtime identity capture. |
